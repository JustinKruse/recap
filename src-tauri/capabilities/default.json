{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window and region-selection overlay",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default"
  ]
}
