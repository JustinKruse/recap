{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window, region-selection overlay, and annotation editor",
  "windows": ["main", "overlay", "editor"],
  "permissions": [
    "core:default"
  ]
}
